export type CardData = {
  cardImage: string;
  cardImageWebP: string;
  altText: string;
  offerLink: string;
  title: string;
  text: string;
};

export type FrontCardProps = {
  data: CardData;
};
